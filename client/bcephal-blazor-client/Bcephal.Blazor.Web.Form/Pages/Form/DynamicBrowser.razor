@boby

@code{


    public RenderFragment boby => __builder =>
   {
       if (EditorData != null)
      {
        <DynamicBrowserComponent EditorData=EditorData P="GridItem" />
      }
};

}